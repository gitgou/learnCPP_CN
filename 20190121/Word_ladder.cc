 ///
 /// @file    Word_ladder.cc
 /// @author  yll(1711019653@qq.com)
 /// @date    2019-01-24 17:45:47
 ///
#include<vector> 
#include <iostream>
#include<string>
using std::cout;
using std::endl;
using std::string;
using std::vector;

typedef  int length;

length wordLadder(vector<string> dictionary, string begin, string end)
{
	size_t sizeDictionary = dictionary.size();
	size_t sizeWord = dictionary[0].length();
	int differentAlphaSubscript[]
	for(int i = 0; i < sizeWord ; ++i)
	{
		if(begin[i] != end[i])
	}
}

int main(void)
{
 
}
